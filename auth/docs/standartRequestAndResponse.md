# Единая структура ответов API

## 🎯 Общее описание

Все API endpoints во всех микросервисах системы Budget Invest Bloom используют **единую стандартную структуру ответа**.
Это обеспечивает полную консистентность интерфейсов, упрощает обработку ответов на frontend и стандартизирует
логирование и мониторинг системы.

---

## 📋 Структура универсального ответа

Каждый ответ от любого API endpoint содержит следующие обязательные поля:

| Поле        | Тип    | Обязательное | Описание                                                    |
|-------------|--------|--------------|-------------------------------------------------------------|
| `id`        | string | Да           | Уникальный идентификатор запроса в формате UUID             |
| `status`    | number | Да           | HTTP статус код ответа (200, 201, 400, 404, 500 и т.д.)     |
| `message`   | string | Да           | Человекочитаемое сообщение на русском языке                 |
| `timestamp` | string | Да           | Временная метка формирования ответа в ISO 8601 UTC формате  |
| `body`      | object | Нет          | Полезная нагрузка ответа с данными (может быть null или {}) |

---

## 📝 Подробное описание полей

### 🔸 Поле `id`

- **Назначение**: Уникальный идентификатор каждого запроса для трейсинга и отладки
- **Формат**: UUID v4 (например: `123e4567-e89b-12d3-a456-426614174000`)
- **Генерация**: Создается автоматически на уровне API Gateway для каждого входящего запроса
- **Использование**:
    - Корреляция запросов в логах всех микросервисов
    - Отладка проблем в distributed системе
    - Связывание запроса с метриками мониторинга
    - Передача в службу поддержки для диагностики ошибок

### 🔸 Поле `status`

- **Назначение**: Дублирование HTTP статус кода в теле ответа для удобства обработки
- **Формат**: Целое число (100-599)
- **Значения**: Соответствует стандартным HTTP статус кодам
    - `200` - успешное выполнение операции
    - `201` - успешное создание ресурса
    - `400` - ошибка в запросе клиента
    - `401` - требуется аутентификация
    - `403` - доступ запрещен
    - `404` - ресурс не найден
    - `500` - внутренняя ошибка сервера
- **Использование**:
    - Упрощение обработки ответов в frontend приложениях
    - Логирование и мониторинг успешности операций
    - Автоматическая категоризация ответов в аналитике

### 🔸 Поле `message`

- **Назначение**: Описание результата операции для пользователя или разработчика
- **Формат**: Строка на русском языке
- **Содержание**:
    - **Успешные операции**: краткое подтверждение ("Данные успешно сохранены", "Транзакция создана")
    - **Ошибки**: понятное объяснение проблемы ("Некорректный формат email", "Недостаточно средств")
- **Принципы**:
    - Сообщения должны быть информативными, но не техническими
    - Избегать внутренних системных деталей
    - Максимальная длина: 255 символов
- **Использование**:
    - Отображение уведомлений пользователю
    - Логирование понятных описаний операций
    - Документирование API для разработчиков

### 🔸 Поле `timestamp`

- **Назначение**: Точное время формирования ответа сервером
- **Формат**: ISO 8601 с миллисекундами и UTC timezone (`2025-08-09T14:30:45.123Z`)
- **Генерация**: Устанавливается в момент формирования ответа каждым микросервисом
- **Использование**:
    - Отладка производительности и времени выполнения запросов
    - Синхронизация данных между клиентом и сервером
    - Аудит операций с точной временной привязкой
    - Анализ нагрузки и паттернов использования системы

### 🔸 Поле `body`

- **Назначение**: Основные данные ответа, специфичные для каждого endpoint
- **Формат**: JSON объект или null
- **Содержимое**:
    - **Для GET запросов**: запрашиваемые данные (объект, массив, примитивы)
    - **Для POST запросов**: созданный ресурс с его идентификатором
    - **Для PUT/PATCH**: обновленные данные ресурса
    - **Для DELETE**: подтверждение удаления или null
    - **Для ошибок**: дополнительная информация об ошибке (поле, код ошибки)
- **Особенности**:
    - Может быть `null` для операций без возвращаемых данных
    - Пустой объект `{}` для операций без конкретных данных, но с подтверждением
    - Массив `[]` для пустых коллекций
- **Использование**:
    - Передача полезной нагрузки между клиентом и сервером
    - Основные данные для обработки в frontend приложениях

---

## 💡 Примеры применения

### ✅ Успешное создание ресурса (POST)

```json
{
  "id": "123e4567-e89b-12d3-a456-426614174000",
  "status": 201,
  "message": "Категория успешно создана",
  "timestamp": "2025-08-09T14:30:45.123Z",
  "body": {
    "categoryId": "456e7890-e89b-12d3-a456-426614174000",
    "name": "Продукты питания",
    "type": "EXPENSE",
    "budgetLimit": 5000000,
    "createdAt": "2025-08-09T14:30:45.123Z"
  }
}
```

### ✅ Успешное получение данных (GET)

```json
{
  "id": "789e0123-e89b-12d3-a456-426614174000",
  "status": 200,
  "message": "Данные категории получены",
  "timestamp": "2025-08-09T14:32:15.456Z",
  "body": {
    "categoryId": "456e7890-e89b-12d3-a456-426614174000",
    "name": "Продукты питания",
    "type": "EXPENSE",
    "budgetLimit": 5000000,
    "currentSpent": 3250000,
    "isActive": true,
    "createdAt": "2025-08-01T10:00:00.000Z",
    "updatedAt": "2025-08-09T14:30:45.123Z"
  }
}
```

### ✅ Успешное получение списка (GET с коллекцией)

```json
{
  "id": "abc1234d-e89b-12d3-a456-426614174000",
  "status": 200,
  "message": "Список транзакций получен",
  "timestamp": "2025-08-09T14:35:22.789Z",
  "body": {
    "items": [
      {
        "transactionId": "111e2222-e89b-12d3-a456-426614174000",
        "description": "Покупка продуктов",
        "amount": 250000,
        "date": "2025-08-09T10:00:00.000Z"
      },
      {
        "transactionId": "333e4444-e89b-12d3-a456-426614174000",
        "description": "Оплата интернета",
        "amount": 80000,
        "date": "2025-08-08T15:30:00.000Z"
      }
    ],
    "pagination": {
      "page": 1,
      "size": 20,
      "totalElements": 156,
      "totalPages": 8
    }
  }
}
```

### ✅ Успешное удаление (DELETE)

```json
{
  "id": "def5678e-e89b-12d3-a456-426614174000",
  "status": 200,
  "message": "Транзакция успешно удалена",
  "timestamp": "2025-08-09T14:40:10.234Z",
  "body": {
    "deletedTransactionId": "111e2222-e89b-12d3-a456-426614174000",
    "deletedAt": "2025-08-09T14:40:10.234Z"
  }
}
```

### ❌ Ошибка валидации (400 Bad Request)

```json
{
  "id": "error123-e89b-12d3-a456-426614174000",
  "status": 400,
  "message": "Некорректные данные запроса",
  "timestamp": "2025-08-09T14:45:33.567Z",
  "body": {
    "errorCode": "VALIDATION_ERROR",
    "field": "email",
    "details": "Некорректный формат email адреса",
    "providedValue": "invalid-email"
  }
}
```

### ❌ Ошибка аутентификации (401 Unauthorized)

```json
{
  "id": "auth-err1-e89b-12d3-a456-426614174000",
  "status": 401,
  "message": "Требуется аутентификация",
  "timestamp": "2025-08-09T14:50:45.890Z",
  "body": {
    "errorCode": "AUTHENTICATION_REQUIRED",
    "loginUrl": "/api/auth/login"
  }
}
```

### ❌ Ресурс не найден (404 Not Found)

```json
{
  "id": "notfound1-e89b-12d3-a456-426614174000",
  "status": 404,
  "message": "Категория не найдена",
  "timestamp": "2025-08-09T14:55:12.345Z",
  "body": {
    "errorCode": "CATEGORY_NOT_FOUND",
    "requestedId": "nonexistent-category-id",
    "resourceType": "BUDGET_CATEGORY"
  }
}
```

### ❌ Внутренняя ошибка сервера (500 Internal Server Error)

```json
{
  "id": "server-er-e89b-12d3-a456-426614174000",
  "status": 500,
  "message": "Внутренняя ошибка сервера",
  "timestamp": "2025-08-09T15:00:30.678Z",
  "body": {
    "errorCode": "INTERNAL_SERVER_ERROR",
    "supportContact": "support@budgetinvestbloom.ru"
  }
}
```

---

## 🔧 Техническая реализация

### Создание на уровне API Gateway

- Поле `id` генерируется Spring Cloud Gateway для каждого входящего запроса
- Передается во все downstream сервисы через HTTP заголовок `X-Request-ID`
- Используется во всех логах для трейсинга

### Формирование в каждом микросервисе

- Каждый сервис использует общий `ResponseWrapper<T>` класс
- Автоматическое заполнение `status`, `message`, `timestamp`
- Полезная нагрузка помещается в поле `body`

### Пример Java класса

```java

@JsonInclude(JsonInclude.Include.NON_NULL)
public class ApiResponse<T> {
    private String id;
    private Integer status;
    private String message;
    private String timestamp;
    private T body;

    // constructors, getters, setters
}
```

---

## ✅ Преимущества единой структуры

1. **Консистентность**: Все API работают одинаково
2. **Упрощение frontend**: Единая логика обработки ответов
3. **Трейсинг**: Отслеживание запросов через все сервисы
4. **Мониторинг**: Стандартизированные метрики и алерты
5. **Отладка**: Простая диагностика проблем по `id` запроса
6. **Документирование**: Единый формат для всех endpoint'ов

---

*Этот стандарт является обязательным для всех микросервисов системы Budget Invest Bloom*


---

## 📤 Единая структура запросов API

Все API endpoints во всех микросервисах системы Budget Invest Bloom также используют **единую стандартную структуру
запроса**. Это обеспечивает консистентность обработки входящих данных и упрощает валидацию на всех уровнях системы.

---

## 📋 Структура универсального запроса

Каждый запрос к любому API endpoint содержит следующие обязательные блоки:

| Поле   | Тип    | Обязательное | Описание                                                     |
|--------|--------|--------------|--------------------------------------------------------------|
| `user` | object | Да           | Информация о пользователе, выполняющем запрос                |
| `data` | object | Нет          | Полезная нагрузка запроса с данными (может быть null или {}) |

---

## 📝 Подробное описание блоков запроса

### 🔸 Блок `user`

- **Назначение**: Содержит информацию о пользователе для аутентификации, авторизации и аудита
- **Обязательность**: Присутствует во всех запросах (кроме публичных endpoint'ов типа регистрации)
- **Заполнение**: Автоматически добавляется API Gateway после валидации JWT токена
- **Содержимое**:

| Подполе     | Тип    | Обязательное | Описание                                   |
|-------------|--------|--------------|--------------------------------------------|
| `userId`    | string | Да           | UUID идентификатор пользователя            |
| `email`     | string | Да           | Email адрес пользователя                   |
| `role`      | string | Да           | Роль пользователя (USER, ADMIN, MODERATOR) |
| `sessionId` | string | Да           | Идентификатор текущей сессии пользователя  |

- **Использование**:
    - Проверка прав доступа к ресурсам
    - Фильтрация данных по принадлежности пользователю
    - Аудит операций и логирование действий
    - Персонализация ответов

### 🔸 Блок `data`

- **Назначение**: Основные данные запроса, специфичные для каждого endpoint
- **Формат**: JSON объект или null
- **Содержимое**:
    - **Для POST запросов**: данные для создания нового ресурса
    - **Для PUT/PATCH запросов**: данные для обновления существующего ресурса
    - **Для GET запросов**: параметры фильтрации, пагинации, сортировки
    - **Для DELETE запросов**: дополнительные параметры удаления (может быть пустым)
- **Особенности**:
    - Может быть `null` или `{}` для запросов без дополнительных данных
    - Содержит только бизнес-данные, относящиеся к конкретной операции
    - Валидируется согласно схеме конкретного endpoint'а
- **Использование**:
    - Передача полезной нагрузки от клиента к серверу
    - Основные данные для обработки бизнес-логики

---

## 💡 Примеры структур запросов

### ✅ Создание новой категории бюджета (POST)

```json
{
  "user": {
    "userId": "123e4567-e89b-12d3-a456-426614174000",
    "email": "user@example.com",
    "role": "USER",
    "sessionId": "sess-789e0123-e89b-12d3-a456-426614174000"
  },
  "data": {
    "name": "Продукты питания",
    "type": "EXPENSE",
    "budgetLimit": 5000000,
    "description": "Ежемесячные расходы на продукты",
    "isActive": true
  }
}
```

### ✅ Обновление существующей транзакции (PUT)

```json
{
  "user": {
    "userId": "123e4567-e89b-12d3-a456-426614174000",
    "email": "user@example.com",
    "role": "USER",
    "sessionId": "sess-789e0123-e89b-12d3-a456-426614174000"
  },
  "data": {
    "transactionId": "456e7890-e89b-12d3-a456-426614174000",
    "description": "Покупка продуктов в Пятерочке",
    "amount": 1875000,
    "categoryId": "789e0123-e89b-12d3-a456-426614174000",
    "date": "2025-08-09T10:00:00.000Z",
    "tags": [
      "продукты",
      "супермаркет"
    ]
  }
}
```

### ✅ Получение списка транзакций с фильтрами (GET)

```json
{
  "user": {
    "userId": "123e4567-e89b-12d3-a456-426614174000",
    "email": "user@example.com",
    "role": "USER",
    "sessionId": "sess-789e0123-e89b-12d3-a456-426614174000"
  },
  "data": {
    "filters": {
      "categoryId": "789e0123-e89b-12d3-a456-426614174000",
      "dateFrom": "2025-08-01T00:00:00.000Z",
      "dateTo": "2025-08-31T23:59:59.999Z",
      "minAmount": 100000,
      "maxAmount": 5000000
    },
    "pagination": {
      "page": 1,
      "size": 20
    },
    "sorting": {
      "field": "date",
      "order": "desc"
    }
  }
}
```

### ✅ Удаление транзакции (DELETE)

```json
{
  "user": {
    "userId": "123e4567-e89b-12d3-a456-426614174000",
    "email": "user@example.com",
    "role": "USER",
    "sessionId": "sess-789e0123-e89b-12d3-a456-426614174000"
  },
  "data": {
    "transactionId": "456e7890-e89b-12d3-a456-426614174000",
    "reason": "Ошибочная транзакция"
  }
}
```

### ✅ Простой запрос без дополнительных данных (GET)

```json
{
  "user": {
    "userId": "123e4567-e89b-12d3-a456-426614174000",
    "email": "user@example.com",
    "role": "USER",
    "sessionId": "sess-789e0123-e89b-12d3-a456-426614174000"
  },
  "data": {}
}
```

### ✅ Публичный запрос без аутентификации (POST регистрация)

```json
{
  "user": null,
  "data": {
    "email": "newuser@example.com",
    "password": "securepassword123",
    "acceptTerms": true
  }
}
```

---

## 🔧 Техническая обработка запросов

### На уровне API Gateway

1. **Извлечение JWT токена** из заголовка `Authorization`
2. **Валидация и декодирование токена**
3. **Автоматическое заполнение блока `user`** из токена
4. **Проксирование запроса** в соответствующий микросервис

### В каждом микросервисе

1. **Валидация структуры запроса** (наличие блоков `user` и `data`)
2. **Проверка прав доступа** на основе данных пользователя
3. **Валидация бизнес-данных** в блоке `data`
4. **Обработка бизнес-логики** с учетом контекста пользователя

### Пример Java DTO класса

```java

@JsonInclude(JsonInclude.Include.NON_NULL)
public class ApiRequest<T> {
    private UserContext user;
    private T data;

    // constructors, getters, setters
}

public class UserContext {
    private String userId;
    private String email;
    private String role;
    private String sessionId;

    // constructors, getters, setters
}
```

---

## 🛡️ Безопасность и валидация

### Проверки на уровне Gateway

- **JWT токен обязателен** для всех защищенных endpoint'ов
- **Валидация срока действия** токена и его подписи
- **Извлечение пользовательских данных** из токена
- **Rate limiting** на основе `userId` или IP адреса

### Проверки в микросервисах

- **Соответствие userId** в токене и в запросе
- **Проверка прав доступа** к запрашиваемым ресурсам
- **Валидация принадлежности данных** пользователю
- **Бизнес-валидация** содержимого блока `data`

### Аудит и логирование

- **Логирование всех запросов** с идентификатором пользователя
- **Отслеживание операций** по `sessionId`
- **Мониторинг подозрительной активности**
- **Соответствие GDPR** требованиям

---

## ✅ Преимущества единой структуры запросов

1. **Безопасность**: Автоматическая передача контекста пользователя
2. **Консистентность**: Единая обработка во всех сервисах
3. **Аудит**: Полное отслеживание действий пользователей
4. **Валидация**: Стандартизированная проверка прав доступа
5. **Разработка**: Упрощение создания новых endpoint'ов
6. **Отладка**: Четкая идентификация пользователя в логах

---

## 🔄 Полный цикл запрос-ответ

### Пример: Создание транзакции

**Запрос:**

```json
{
  "user": {
    "userId": "123e4567-e89b-12d3-a456-426614174000",
    "email": "user@example.com",
    "role": "USER",
    "sessionId": "sess-789e0123-e89b-12d3-a456-426614174000"
  },
  "data": {
    "description": "Покупка продуктов",
    "amount": 1875000,
    "categoryId": "789e0123-e89b-12d3-a456-426614174000"
  }
}
```

**Ответ:**

```json
{
  "id": "req-abc1234d-e89b-12d3-a456-426614174000",
  "status": 201,
  "message": "Транзакция успешно создана",
  "timestamp": "2025-08-09T14:30:45.123Z",
  "body": {
    "transactionId": "new-trans1-e89b-12d3-a456-426614174000",
    "description": "Покупка продуктов",
    "amount": 1875000,
    "categoryId": "789e0123-e89b-12d3-a456-426614174000",
    "userId": "123e4567-e89b-12d3-a456-426614174000",
    "createdAt": "2025-08-09T14:30:45.123Z"
  }
}
```

---

*Единые структуры запросов и ответов являются обязательными для всех микросервисов системы Budget Invest Bloom*